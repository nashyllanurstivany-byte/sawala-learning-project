const siswa = {
    nama: "Nashylla",
    kelas: "XII",
    jurusan: "Pengembangan Perangkat Lunak dan Gim",
};

const hobi = ["membaca", "menggambar", "coding"];

// Akses object pakai titik
console.log(siswa.nama);

// Akses array pakai index (dimulai dari 0)
console.log(hobi[0]);

// Tambah properti baru ke object
siswa.umur = 17;

// Cetak seluruh object
console.log(siswa);

// Cetak seluruh array
console.log(hobi);