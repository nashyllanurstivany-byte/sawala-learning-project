// 1. Variabel string (pakai const karena nama nggak akan berubah)
const nama = "Nashylla";

// 2. Variabel number (pakai let karena umur bisa berubah tiap tahun)
let umur = 17;

// 3. Variabel boolean
let statusLulusSekolah = false;

// 4. null vs undefined
let dataKosong = null;
let belumDiisi;

// 5. Cetak semuanya
console.log(`Nama saya ${nama}, umur ${umur}`);
console.log(`Status lulus Sekolah: ${statusLulusSekolah}`);
console.log(dataKosong);
console.log(belumDiisi);